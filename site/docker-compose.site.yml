services:
  site:
    build:
      context: .
      dockerfile: Dockerfile.brixter
      args:
        BRIXTER_VARIANT: site
    env_file:
      - .env.site
    environment:
      BRIXTER_VARIANT: site
      ORIGIN: ${ORIGIN:-http://localhost:5173}
    ports:
      - '5173:3000'
