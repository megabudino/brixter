# The public site renders `.brix.yaml` pages via @brixter/core and needs no
# server, auth, database, or GitHub configuration. Content is edited by running
# the separate Brixter Studio app (apps/studio), not this site.

PUBLIC_DEBUG=false
