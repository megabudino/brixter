name: Test

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.15

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          package-manager-cache: false

      - run: bun install --frozen-lockfile

      # `packages/brixter/tsconfig.json` extends `./.svelte-kit/tsconfig.json`,
      # which `svelte-kit sync` generates and `.gitignore` excludes. Without it
      # Vite cannot resolve the tsconfig and eight spec files fail to load,
      # before a single test runs.
      - name: Generate the SvelteKit tsconfig the package extends
        run: bunx svelte-kit sync
        working-directory: packages/brixter

      - name: Unit tests
        run: bun run test

      # The published packages ship compiled JS; a type error there is a broken
      # release, so both builds run even though the tests import from source.
      - name: Build packages
        run: bun run build:brixter

      # The demo site is the end-to-end check: it exercises the page compiler,
      # the schema validator and the renderer against real content.
      - name: Check the demo site
        run: node ../packages/brixter/bin/brixter.mjs check
        working-directory: site

      - name: Build the demo site
        run: bun run build
        working-directory: site
